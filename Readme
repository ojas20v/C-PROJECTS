#README

These are projects made by me based on C language, which uses selective standard libraries, 
and these projects denote the usage of basic c, static memeory, dynamic allocation using pointers, all sorts of lists.
